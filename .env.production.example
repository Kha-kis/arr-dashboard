# Arr Dashboard - Docker Production Configuration
#
# ============================================================================
# No configuration required!
# ============================================================================
# This file exists for reference only. When running with Docker Compose,
# all necessary configuration is auto-generated on first startup.
#
# Security keys are automatically generated and saved to the Docker volume.
# All settings use production-ready defaults.
#
# ============================================================================
# Advanced: Custom Configuration (Optional)
# ============================================================================
# Only needed for:
# - Migrating from another installation (preserve encryption keys)
# - Corporate compliance (provide your own keys)
# - Custom ports or network configuration
#
# To customize, create a .env file with your overrides:
#
# # Custom encryption keys (for migration/backup)
# ENCRYPTION_KEY=your-64-char-hex-string
# SESSION_COOKIE_SECRET=your-64-char-hex-string
#
# # Custom ports (if 3000/3001 are in use)
# # Then update docker-compose.yml port mappings accordingly
#
# Generate keys with: openssl rand -hex 32
