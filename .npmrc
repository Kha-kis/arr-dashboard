# pnpm 10 configuration
# Inject workspace packages instead of symlinking them
# Required for `pnpm deploy` to create hermetic deployments
inject-workspace-packages=true

# Keep npm-based tooling aligned with the pnpm release-age policy.
min-release-age=7
