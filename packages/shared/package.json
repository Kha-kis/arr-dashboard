{
  "name": "@arr/shared",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --format cjs --dts",
    "dev": "tsup src/index.ts --watch --format esm --format cjs --dts",
    "lint": "biome lint .",
    "format": "biome format .",
    "test": "echo 'No tests in shared package'",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./package.json": "./package.json"
  }
}
