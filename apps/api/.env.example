# Arr Dashboard - Development Configuration

# ============================================================================
# Database (Optional - Auto-configured based on NODE_ENV)
# ============================================================================
# Development default: file:./dev.db
# Production default: file:/app/data/prod.db
#
# DATABASE_URL="file:./dev.db"

# ============================================================================
# Security Keys (Optional - Auto-generated if not provided)
# ============================================================================
# Leave these commented out to use auto-generated keys
# Only set them if you need specific keys for testing/development
#
# ENCRYPTION_KEY=replace-with-32-byte-hex-key
# SESSION_COOKIE_SECRET=replace-with-32-byte-hex-secret
#
# Generate with: openssl rand -hex 32

# ============================================================================
# Server Configuration (Optional - Has defaults)
# ============================================================================
# NODE_ENV=development
# API_HOST=0.0.0.0
# API_PORT=3001
# API_CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ============================================================================
# Rate Limiting (Optional - Has defaults)
# ============================================================================
# API_RATE_LIMIT_MAX=200
# API_RATE_LIMIT_WINDOW=1 minute

# ============================================================================
# Session Configuration (Optional - Has defaults)
# ============================================================================
# SESSION_COOKIE_NAME=arr_session
# SESSION_TTL_HOURS=24

# ============================================================================
# Frontend URL (Optional - Has default)
# ============================================================================
# APP_URL=http://localhost:3000

# ============================================================================
# WebAuthn / Passkey Configuration (Optional - Has defaults)
# ============================================================================
# For passkey authentication (WebAuthn)
# IMPORTANT: In production, set these to match your domain
#
# WEBAUTHN_RP_NAME=Arr Dashboard
# WEBAUTHN_RP_ID=localhost                        # Your domain (no protocol, no port)
# WEBAUTHN_ORIGIN=http://localhost:3000           # Full URL with protocol
#
# Production example:
# WEBAUTHN_RP_ID=arr.example.com
# WEBAUTHN_ORIGIN=https://arr.example.com

# ============================================================================
# OIDC Authentication (Optional - Database-configured)
# ============================================================================
# OIDC providers (Authelia, Authentik, Generic) are configured via:
# - Initial setup UI: /setup (when no users exist)
# - Admin settings: /settings → OIDC Providers tab (after setup)
#
# No environment variables needed - all OIDC config stored in database

# ============================================================================
# TMDB Configuration (Optional)
# ============================================================================
# TMDB API keys are set per-user in Settings → Account tab
# These URLs rarely need to be changed:
#
# TMDB_BASE_URL=https://api.themoviedb.org/3
# TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p

# ============================================================================
# GitHub API Token (Optional - For TRaSH Guides)
# ============================================================================
# Increases GitHub API rate limit from 60 to 5,000 requests/hour.
# Only needed if you encounter rate limiting with TRaSH Guides sync.
# Create a token at: https://github.com/settings/tokens (no scopes needed)
#
# GITHUB_TOKEN=ghp_your_token_here
